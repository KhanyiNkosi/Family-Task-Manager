"use client";

import { useState } from 'react';

export default function Home() {
  const [activeFeature, setActiveFeature] = useState<number | null>(null);

  const features = [
    {
      id: 1,
      icon: '?',
      title: 'Manage Tasks',
      description: 'Easily create and assign tasks to family members. Keep track of what needs to be done.',
      detailedDescription: 'Create, assign, and track family chores with our intuitive dashboard. Set deadlines and monitor progress in real-time.'
    },
    {
      id: 2,
      icon: '?',
      title: 'Motivate with Rewards',
      description: 'Set up a points system for completed tasks. Let your kids earn exciting rewards.',
      detailedDescription: 'Turn chores into games! Kids earn points for completed tasks, unlocking rewards and building positive habits.'
    },
    {
      id: 3,
      icon: '?',
      title: 'Stay Connected',
      description: 'Improve communication and teamwork within the family. Achieve your goals together.',
      detailedDescription: 'Share calendars, leave notes, and celebrate achievements together. Strengthen family bonds through cooperation.'
    }
  ];

  const handleFeatureClick = (id: number, title: string, description: string) => {
    setActiveFeature(id);
    alert(`${title}\n\n${description}`);
    setTimeout(() => setActiveFeature(null), 2000);
  };

  const handleGetStarted = () => {
    alert('Welcome to FamilyTask! Redirecting to signup...');
    // In real app: router.push('/signup');
  };

  return (
    <main>
      {/* Hero Section */}
      <section className="text-center py-16 md:py-24 lg:py-32 px-4">
        <div className="max-w-4xl mx-auto">
          <h1 className="text-4xl md:text-6xl lg:text-7xl font-black text-primary-cyan 
                         leading-tight md:leading-tight lg:leading-tight mb-6">
            Organize Your Family's Life
          </h1>
          
          <p className="text-lg md:text-xl lg:text-2xl text-text-dark mb-10 md:mb-12 
                       max-w-2xl mx-auto leading-relaxed">
            The fun and easy way to manage tasks, rewards, and family cooperation.
          </p>
          
          <button
            onClick={handleGetStarted}
            className="bg-primary-cyan text-white font-bold px-10 py-4 md:px-12 md:py-5
                     rounded-xl shadow-cyan-glow hover:shadow-cyan-glow-hover
                     hover:translate-y-[-3px] active:translate-y-0
                     transition-all duration-300 text-lg md:text-xl"
          >
            Get Started for Free
          </button>
        </div>
      </section>

      {/* Features Section */}
      <section id="features" className="py-16 md:py-24 px-4">
        <div className="max-w-7xl mx-auto">
          <div className="grid grid-cols-1 md:grid-cols-3 gap-6 md:gap-8 mb-16">
            {features.map((feature) => (
              <div
                key={feature.id}
                onClick={() => handleFeatureClick(feature.id, feature.title, feature.detailedDescription)}
                className={`
                  bg-card-bg p-8 rounded-3xl cursor-pointer feature-card-hover
                  ${activeFeature === feature.id ? 'ring-2 ring-primary-cyan ring-opacity-50' : ''}
                  transition-all duration-300
                `}
              >
                <div className="w-16 h-16 bg-primary-cyan bg-opacity-10 rounded-2xl 
                              flex items-center justify-center text-3xl font-bold 
                              text-primary-cyan mb-6">
                  {feature.icon}
                </div>
                
                <h3 className="text-2xl md:text-3xl font-bold text-primary-cyan mb-4">
                  {feature.title}
                </h3>
                
                <p className="text-text-dark text-base md:text-lg leading-relaxed">
                  {feature.description}
                </p>
              </div>
            ))}
          </div>

          {/* Divider */}
          <div className="h-px bg-gradient-to-r from-transparent via-primary-cyan/20 to-transparent 
                         my-12 md:my-16"></div>

          {/* Footer */}
          <footer className="text-center py-8">
            <p className="text-text-dark font-medium">
              © {new Date().getFullYear()} FamilyTask. All rights reserved.
            </p>
          </footer>
        </div>
      </section>
    </main>
  );
}
